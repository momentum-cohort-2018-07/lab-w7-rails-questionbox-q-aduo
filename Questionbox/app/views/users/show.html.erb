# trying to show all of a user's questions on a user profile page.

#add code to find by user id?
# <%= form.hidden_field :user_id, value: current_user.id %>

<h1><%= @user.username %>'s Questions</h1>
<table>
  <thead>
    <tr>
      <th>Username</th>
      <th>Question</th>
    </tr>
  </thead>
  <tbody>
  <% @user.each do |user| %>
    <tr>
      <td><%= user.username %></td>
      <td><%= user.question %></td>
    </tr>
  <% end %>
  </tbody>
</table>


<%= link_to 'Back', root_path %>